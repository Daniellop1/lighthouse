name: Audit live site
on: push

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
    - name: Audit live URL
      uses: daniellop1/lighthouse
      with:
        url: 'https://jarv.is/'
    - name: Upload results as an artifact
      uses: daniellop1/lighthouse
      with:
        name: report
        path: './report'
